/*
 * Note how this route is structured inside the 'admin' folder/component
 */

export default function AdminPostsPage({}) {
    return(
        <main>
            <h1> Sign Up </h1>
        </main>
    )
}