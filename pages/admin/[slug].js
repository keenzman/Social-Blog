/* A dynamic route is used when many diff routes point to the same component.
    e.g. admin/hello-world and admin/something-else
 * Note that Static routes have priority over self made dynamic routes.
 */
export default function AdminPostEdit({}) {
    return(
        <main>
            <h1> Sign Up </h1>
        </main>
    )
}